/*
package project.aurora.auth.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;
import project.aurora.auth.models.OAuth2Provider;
import project.aurora.auth.models.User;

import java.util.UUID;

@Repository
public interface OAuth2ProviderRepository extends JpaRepository<OAuth2Provider, UUID> {
    OAuth2Provider findByProviderNameAndProvidedUserId(String providerName, String providedUserId);
}
*/
